version: 2

models:
  - name: int_pdt_session_events_agg
    description: Event type by session id and user
    columns:
      - name: session_id
        description: session_id of the event. This id is not unique
        tests:
        - not_null
  - name: fct_pdt_sessions
    description: Event type by session id and user
    columns:
      - name: session_id
        description: session_id of the event. This id is not unique
        tests:
        - not_null
